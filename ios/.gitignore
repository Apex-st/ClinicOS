App/build
App/Pods
App/output
DerivedData
xcuserdata

# Cordova plugins for Capacitor
capacitor-cordova-ios-plugins

# config.xml и capacitor.config.json нужны в Xcode — не игнорируем.

# Web-сборка 2.24 лежит в App/App/public — её нужно открывать в Xcode без npm.
# Не игнорируем public/.
